# Personal website release — 2026-09-17

The author explicitly requested a push after reviewing the local website, with
three retained appearances: classical, day, and night. This supersedes earlier
local-only holds in the historical review notes.

## Scope

- Release the reviewed scholarly homepage, Writing, Translations & Notes,
  Mentors, Friends, and web CV changes.
- One appearance menu: Ink & Paper / Day / Night. Stored as `elvis-appearance`;
  older color/style preferences are migrated. Day is plain white, Night is plain
  dark, and Ink & Paper uses the generated landscape. No separate conflicting
  color toggle is loaded.
- The author deferred the proposed additional friend before release. The final
  Friends page contains only Yating Zou; the site test guards this boundary.
- Preserve Yating Zou's acknowledgement and link. The outgoing-link label is
  now gender-neutral; optional closing text is only rendered when present.
- Preserve existing bibliography and all translation rights boundaries. Only
  the previously reviewed CC BY 4.0 Advani translation PDF is newly hosted.
  No private source CV, restricted translation, administrative document or
  Qian Lab internal file is included.

## Checks before push

- Jekyll build passed; site checker passed 11 routes and 275 local links/assets.
- Three-mode unit tests: initial state before paint, all three selections,
  persistence, old-preference migration, invalid values, blocked storage,
  missing controls.
- Browser screenshot review: desktop 1440 px for Day, Night and Ink & Paper;
  phone 390 px for Friends in Ink & Paper and Night. Day also measured at 390 px.
- No horizontal overflow in the inspected desktop/mobile states. Reload retains
  Night, and navigating from About to Friends retains the selected appearance.
- Optional Chinese friend names can render on a separate secondary line;
  optional closing text is omitted when absent.
- `git diff --check` passed. `_site-preview` is ignored and not committed.

## Release target

Repository: `ElvisCuiHan/ElvisCuiHan.github.io`.
GitHub Pages source verified via API: `master`, repository root, legacy build.
Remote base before release: `80073ad`. Push must be fast-forward, never forced.

The separate full-redesign worktree, Qian Lab website, UNC-UCLA source and
course repository are outside this release and remain untouched.
