# Personal-site refinement release — 2026-09-18

The author explicitly requested adding Gang Li, arranging the mentors, exposing
their links and pushing this update. This authorization also publishes the
immediately preceding local refinements requested by the author; no unrelated
project is changed. The destination remains the existing repository's `master`
branch and GitHub Pages at https://elviscuihan.github.io/.

## Scope

- Eight Mentors buttons, ordered Zhejiang University → UCLA → Westlake.
  Gang Li follows Weng Kee Wong; his memory reads “Professor Li taught me survival
  analysis.” Every teacher has a separate, visible external-reference link.
- External Links contains only the Yating Zou styling-thanks button. No repeated
  homepage acknowledgement or rendered institution directory; archived assets
  and institutional data are retained.
- Shared header and homepage display the English name only, as requested.
- Author-confirmed landscape header logo and seal favicon; unchanged bamboo
  variant retained for future use and phoenix retained as an alternate.
- Three appearances, selected-paper screenshots, bibliography, independent
  pages, WeChat, card and existing content-access boundaries remain unchanged.

## Verification before publication

- Jekyll build passed.
- Site checker passed 10 pages and 282 local links/assets, including the eight
  mentor memories, order, independent links, one acknowledgement and 22 legacy
  aliases.
- Three-mode appearance and clipboard tests passed.
- Mentor references retrieved successfully (Fang's is a historical news
  reference, not an official faculty profile); no external biographies copied.
- `git diff --check` passed; build/cache output is not included.
- Remote fetched before release; publication uses a normal fast-forward push.

## Remaining limits

Browser connection timed out, so there are no new browser screenshots or actual
keyboard/click and responsive-layout checks for these refinements. Earlier
screenshots document earlier editions only. Automated tests and HTTP inspection
are not represented as visual acceptance.

Earlier “local only / no push” notes describe the state when they were written;
this explicit authorization supersedes those holds, not their QA limitations
or third-party rights notices. Deployment success is to be verified separately
from the Git push.
